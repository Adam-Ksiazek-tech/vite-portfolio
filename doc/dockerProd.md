# docker PROD

docker compose -f docker-compose.prod.yml up -d --build

## Aktualizacja

wejdź na serwer, przejdź do folderu vite...

przebuduj image

docker compose -f docker-compose.prod.yml up -d --build